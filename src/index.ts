type PositiveTemporal =
  | { days: string }
  | { weeks: string }
  | { months: string }
  | { years: string }
  | { hours: string }
  | { minutes: string };

export const toPositiveTemporal = (amount, unit, nonNegative) => {
  // your code here
  return {};
};
